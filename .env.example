# .env.example
# Copy this file to .env and fill in your values. Do NOT commit .env to source control.

GCP_PROJECT_ID="your-project-id"
GCP_REGION="australia-southeast1"
GCS_BUCKET_NAME="globaltech-ceo-uploads"
SERVICE_NAME="ceo-dashboard"

# Ensure GEMINI_API_KEY is stored in Google Secret Manager as "GEMINI_API_KEY"
# For local testing, you can uncomment and add it here:
# GEMINI_API_KEY="your-api-key"
